/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
     domains: ['api.amalys.pro'],
    remotePatterns: [
      
    ],
  },
};

module.exports = nextConfig;
